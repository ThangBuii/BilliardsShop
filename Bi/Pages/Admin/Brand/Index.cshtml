@page
@model Client.Pages.Admin.Brand.IndexModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = null;
}

<head>
    <title>Brand</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="d-flex justify-content-center align-items-center" style="height: 100vh;">
        <div class="container">
            <div class="card p-4 shadow">
                <h1 class="mb-4">Brand</h1>

                <!-- Search Section -->
                <div class="row mb-3">
                    <div class="col-md-12">
                        <form class="d-flex" method="get">
                            <input class="form-control me-1" type="search" name="searchTerm" value="@Model.SearchTerm" placeholder="Search by name" aria-label="Search">
                            <button class="btn btn-secondary" type="submit">Search</button>
                        </form>
                    </div>
                </div>

                <!-- Add Button -->
                <div class="row mb-3">
                    <div class="col-md-12">
                        <a href="/Admin/Brand/Create" class="btn btn-primary">Add</a>
                    </div>
                </div>

                <!-- Table Section -->
                <div class="row">
                    <div class="col-md-12">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th class="col-md-1">Id</th>
                                    <th class="col-md-1">Name</th>
                                    <th class="col-md-1">Feature</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach(var listBrand in Model.Brands)
                                {
                                <tr>    
                                    <td>@listBrand.Id</td>
                                    <td>@listBrand.Name</td>
                                    <td>
                                        <a href='/Admin/Brand/Update/@listBrand.Id' class="btn btn-warning btn-sm btn-edit-brand">Edit</a>
                                        <a class="btn btn-danger btn-sm" asp-page-handler="Delete" asp-route-Id="@listBrand.Id">Delete</a>
                                    </td>
                                </tr>
                                }
                            </tbody>
                        </table>
                        <div class="row mb-3">
                            <div class="col-md-12 text-end">
                                <a href="/Admin/Index" class="btn btn-secondary">Menu Admin</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>