@page
@model Client.Pages.Admin.User.IndexModel
@{
}

<body class="bg-light">
    <div class="container mt-4">
        <h1 class="mb-4">User</h1>
        <div class="my-3 p-3 bg-body rounded shadow-sm">
            <!-- Search -->
            <div class="pb-3">
                <form class="d-flex" action="" method="get">
                    <input class="form-control me-1" type="search" name="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-secondary" type="submit">Search</button>
                </form>
            </div>

            <!-- Add -->
            <div class="pb-3">
                <a href="/Admin/User/Create" class="btn btn-primary btn-add-user">Add</a>
            </div>

            <!-- Table -->
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th class="col-md-1">Id</th>
                        <th class="col-md-2">Name</th>
                        <th class="col-md-2">Account</th>
                        <th class="col-md-2">Password</th>
                        <th class="col-md-2">Feature</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>1</td>
                        <td>John Doe</td>
                        <td>john.doe@example.com</td>
                        <td>••••••••</td>
                        <td>
                            <a href="/Admin/User/Update" class="btn btn-warning btn-sm btn-edit-user">Edit</a>
                            <a href="#" class="btn btn-danger btn-sm">Delete</a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
  
</body>
